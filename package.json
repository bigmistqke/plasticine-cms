{
  "name": "plasticine",
  "version": "0.0.1",
  "private": true,
  "description": "Git-based CMS with schema migrations",
  "type": "module",
  "scripts": {
    "cli": "tsx packages/cli/src/index.ts",
    "migrate": "tsx packages/cli/src/index.ts migrate",
    "migrate:dry": "tsx packages/cli/src/index.ts migrate --dry-run"
  },
  "license": "MIT",
  "devDependencies": {
    "tsx": "^4.21.0"
  }
}
